let initialState = {
    friends: [
        { id: 1, name: 'Eva' },
        { id: 2, name: 'Eva' },
        { id: 3, name: 'Eva' }
      ]
};
export const sidebarReducer = (state = initialState, action) => {
    return state;
}